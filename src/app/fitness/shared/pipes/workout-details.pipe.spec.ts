import { WorkoutDetailsPipe } from './workout-details.pipe';

describe('WorkoutDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new WorkoutDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
