import { ToggleRemoveActionsDirective } from './toggle-remove-actions.directive';

describe('ToggleRemoveActionsDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleRemoveActionsDirective();
    expect(directive).toBeTruthy();
  });
});
